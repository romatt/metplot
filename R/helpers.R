# Some important stuff
#
# Prepare for development
# pacman::p_load(devtools, usethis, roxygen2, testthat, knitr, rmarkdown)
#
# We first always want to delete the default "NAMESPACE" file, in order for
# Roxygen to be able to take control of it
#
# Use a new package in my package (dependencies)
# use_package("ggplot2")
#
# After updating any function documentation, run
# document()
#
# to set up a new vignette, run
# use_vignette("Intro")
